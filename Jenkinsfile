pipeline {

    stages {
        stage ('Checkout') {
            steps {
                sh "echo 'hello'"
            }
        }
        stage ('abc') {
            steps {
                sh "ls -la"
            }
        }
    }
}